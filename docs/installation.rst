.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install stockfighter

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv stockfighter
    $ pip install stockfighter
